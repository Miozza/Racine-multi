{
  "//": "checkJs est false volontairement : la verification est opt-in, fichier par fichier, via '// @ts-check' en tete. Un fichier a 0 erreur est un fil-piege utile ; un fichier a 30 erreurs est du papier peint. Le domaine charge est active en premier. scripts/globals.d.ts declare les portes window.* que le modele par balises <script> rend invisibles a l editeur.",

  "compilerOptions": {
    "allowJs": true,
    "checkJs": false,
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "strict": false,
    "noEmit": true
  },
  "include": [
    "app.js",
    "scripts/**/*.js",
    "scripts/**/*.d.ts",
    "programs/**/*.js"
  ],
  "exclude": [
    "node_modules",
    "dev/**",
    ".git"
  ]
}
